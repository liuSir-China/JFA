# JFA default config — data stays local (outbound_enabled=false).
evidence.root=${user.home}/.jfa/evidence
retention.days=7
min.free.bytes=1073741824
min.free.ratio=0.05
require.confirm=true
trading.hours.policy=force_confirm
# trading.hours=09:30-15:00
outbound.enabled=false
